# Dawaei — دوائي

Static MVP website for **Dawaei**, a smart healthcare application for finding medicines, nearby pharmacies, price comparison, and medication reminders.

## Repository contents

- `index.html` — the public entry page.
- `outputs/dawaei-mvp.html` — the complete responsive Dawaei website.
- `server.js` and `package.json` — local development server for VS Code.
- `vercel.json` — optional Vercel configuration.

## Run locally in VS Code

1. Open this folder in VS Code.
2. Open the integrated terminal (`Ctrl + \``).
3. Run:

```bash
npm start
```

4. Open `http://localhost:3000` in your browser.

No `npm install` is needed; the project uses Node.js only.

## Deploy on Vercel

1. Create a new GitHub repository.
2. Push this project to the repository.
3. In Vercel, select **Add New → Project → Import Git Repository**.
4. Select this repository and click **Deploy**. No build command is required.

The published home page is `index.html`; it opens the Dawaei interface in `outputs/dawaei-mvp.html`.
"# dawaei" 
